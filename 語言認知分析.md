# `Voice/utils.py` 說明：語言表達認知分析演算法

> 適用檔案：`Voice/utils.py`
> 相關檔案：`Voice/models.py`（`CognitiveAnalysis`）、`Voice/views.py`（`vision_firstQ`、`update_diary_audio`、`upload_chat_voice`、`finalize_diary`、`dashboard`）
> 對應修正文件：`認知分析演算法_修正方向.md`（1.1 ~ 2.3 已全部套用）、`對話流程重構_圖片辨識與聊天室化.md`（聊天室重構後，分析改在 `finalize_diary` 對合併文字執行一次）

---

## 整體流程

聊天室改版後，語言分析不再是「錄一次音就分析一次」，而是等 4 輪對話都收集完，用合併後的完整內容分析一次：

```
聊天室（templates/voice.html）
  ① vision_firstQ         → 照片辨識生成首問，建立 AiConversation
  ② update_diary_audio    → 第 1 輪回覆：Whisper 轉文字，只存進 AiConversation，還不分析
  ③ upload_chat_voice ×3  → 第 2～4 輪回覆：同樣只存進 AiConversation
  ④ finalize_diary        → 合併 4 段使用者回覆文字
                               → assess_sample_quality(combined)   ← 品質門檻
                                   ├─ 不合格 → CognitiveAnalysis(is_valid=False, invalid_reason=...)，不計分
                                   └─ 合格   → analyze_transcription(combined)
                                                 → CognitiveAnalysis(is_valid=True, 六項分數 + total_score + risk_level ...)
  → dashboard view 只讀取 is_valid=True 的紀錄做平均分數 / 語言活力指數（LVI）估算
```

核心原則不變：**先判斷這段文字「值不值得分析」，再判斷「分析結果好不好」。** 空錄音、雜音、Whisper 幻覺輸出會被擋在 `assess_sample_quality` 這一關，不會被誤判成「語言表達高風險」。改成合併 4 段文字才分析，也讓樣本長度更接近 CIU 研究建議的語言樣本量，門檻更不容易被單段簡短錄音誤觸發。

---

## 1. 文字前處理

| 函式 | 用途 |
|---|---|
| `normalize_text(text)` | 去除頭尾空白與所有空白字元，統一格式 |
| `get_content_length(text)` | 計算中文字／英文字母／數字的數量，標點不算 |
| `collapse_repetitions(text)` | 壓縮連續重複的字元／短語（見下方說明） |
| `get_effective_length(text)` | `get_content_length(collapse_repetitions(text))`，扣除跳針重複後的實際內容長度 |
| `count_sentence_markers(text)` | 計算逗號、句號、問號等標點數量，粗略判斷句子/語意片段數 |

### `collapse_repetitions` 的判斷規則

- 單一字元連續 **3 次以上** 才視為口吃／跳針，收斂成 1 個字（例如「水水水水水」→「水」）。
- 2~6 字的短語連續重複也會收斂成 1 次（例如「出來了出來了出來了」→「出來了」）。
- 門檻設在 3 次，是為了不誤傷中文正常的疊字用法（謝謝、剛剛、慢慢、常常）。

`get_effective_length` 與原始 `get_content_length` 的差距會算成 `repetition_ratio`，用於流暢度扣分（見第 3 節）。

---

## 2. 樣本品質檢查：`assess_sample_quality()`

```python
assess_sample_quality(raw_text, whisper_result=None, audio_seconds=None)
# 回傳："ok" / "too_short" / "suspect_hallucination" / "low_confidence"
```

檢查順序：

1. **`too_short`**：正規化後內容長度 < `MIN_CONTENT_LENGTH`（15 字），或 `audio_seconds` < `MIN_AUDIO_SECONDS`（5 秒，目前 views.py 尚未傳入音檔秒數，此檢查暫時不會觸發）。
2. **`suspect_hallucination`**：命中 `WHISPER_HALLUCINATION_PATTERNS`（「謝謝觀看」「請訂閱」「字幕組」等 Whisper 常見的中文語音辨識幻覺字幕語）。
3. **`low_confidence`**：若有傳入 Whisper 的 `result["segments"]`，計算平均 `avg_logprob`（信心值過低 < -1.0）、平均 `no_speech_prob`（>0.6 極可能是靜音/雜音）、最大 `compression_ratio`（>2.4 代表重複迴圈，也是幻覺常見特徵，此情況回傳 `suspect_hallucination`）。
4. 都沒中 → `ok`，才可以呼叫 `analyze_transcription`。

`Voice/views.py` 的 `finalize_diary`（聊天室按下「生成日記」時觸發）會依這個結果決定：
- `quality != "ok"` → 建立 `CognitiveAnalysis(is_valid=False, invalid_reason=quality)`，六項分數留 `null`。
- `quality == "ok"` → 正常呼叫 `analyze_transcription` 並存分數。

`finalize_diary` 呼叫 `assess_sample_quality` 時只傳合併後的文字，不帶 `whisper_result`（4 段錄音各自的信心指標沒有一併保留下來），所以 `low_confidence` 這個分支目前實際上不會在 `finalize_diary` 這條路徑被觸發，只有 `too_short` 和 `suspect_hallucination` 兩種情況生效。

---

## 3. 類別命中：`tag_all_categories()`

```python
tag_all_categories(text, category_words)
# category_words: {"person": [...], "location": [...], "event": [...], ...}
# 回傳：{"person": [...], "location": [...], ...} 每個類別實際命中的詞
```

用**單次掃描**取代「每個類別各自獨立比對」，解決兩個問題：

- **跨類別重複命中**：例如「家人」不該同時被算進人物（「家人」）與地點（「家」）。
- **子字串誤判**：例如「買菜」不該被拆成事件（「買」）+ 物品（「菜」）。

排序規則（`CATEGORY_PRIORITY`）：詞彙依**長度由長到短**比對；長度相同時依 `event > location > object > person > time > emotion > connector` 的優先序決定歸類；一旦某段文字被較長的詞命中，就會用等長空白取代，避免被更短的詞從中間重複命中。這讓結果是決定性的（deterministic），不受 dict 插入順序影響。

呼叫端（`analyze_transcription`）把 7 個詞庫（`person_words`、`location_words`、`event_words`、`object_words`、`emotion_words`、`time_words`、`connector_words`）一次丟進 `tag_all_categories`，取回各自的命中清單。

### 詞庫設計原則

- **避免單字詞**：`event_words` 移除了「送、拿、放、買、煮、玩」，`object_words` 移除了「花、狗、貓、樹、飯、菜、藥」，`emotion_words` 移除了「累」。這些單字太容易變成別的詞（放假、買單、拿手、疲累…）裡的一部分，被誤判成獨立的事件/物品/情緒。
- 移除單字後都保留或新增了對應的多字詞（例如「買菜」「小狗」「花朵」「疲累」），並補了一些長輩常見活動（回家、打掃、澆水、看診、復健、帶孫子…）。
- `person_words` 保留單字「我」，但在**資訊量維度**計分時會排除「我／自己／本人」這種只表示第一人稱的泛稱（見 `substantive_person_matches`），因為它幾乎每句話都會出現，不代表真的提到了「人物」這個資訊維度。

---

## 4. 六大維度計分規則

`analyze_transcription(text)` 回傳一個 dict，包含以下六項分數（每項 0~4 分）：

| 維度 | key | 計分依據 |
|---|---|---|
| 流暢度 | `fluency_score` | `effective_length`（扣除跳針重複後的長度）+ 猶豫詞數量；`repetition_ratio >= 0.30` 額外扣 1 分（最低 0） |
| 資訊量 | `information_score` | 人物（排除泛稱）／地點／事件／物品／情緒／時間，命中幾種類別；`{0:0,1:1,2:2,3:3,4:4,5:4,6:4}`，命中 4 種以上就滿分 |
| 句子結構 | `sentence_score` | 是否同時有人物+事件、補充資訊（地點/物品/時間/情緒）數量、連接詞或標點數量 |
| 命名能力 | `naming_score` | 人物+地點+物品去重後的**不同詞彙**數量 |
| 語意完整性 | `semantic_score` | 核心元素（人物/事件/物品）與補充元素（地點/時間/情緒）的組合，只有文字時稱「完整性」而非「正確性」（無法對照照片內容） |
| 整體溝通能力 | `communication_score` | 前五項的加權平均（流暢 20% / 資訊 20% / 句子 20% / 命名 15% / 語意 25%），四捨五入到整數（`math.floor(x + 0.5)`，非 Python 內建的銀行家捨入） |

### 總分與風險等級

```python
total_score = fluency_score + information_score + sentence_score + naming_score + semantic_score
# 滿分 20（五項獨立維度加總，communication_score 是衍生指標，不重複計入）
average_score = round(total_score / 5, 2)
risk_level = get_risk_result(total_score)   # low >=16 / medium >=11 / high 其餘
```

`get_risk_result()` 的門檻（16 / 11）是依 20 分滿分等比例換算舊版 24 分滿分的門檻（19 / 13）而來。

---

## 5. 回傳格式

```python
{
    "fluency_score": int, "information_score": int, "sentence_score": int,
    "naming_score": int, "semantic_score": int, "communication_score": int,
    "total_score": int, "average_score": float,
    "risk_level": "low" | "medium" | "high",
    "suggestion": str,       # 給使用者看的建議文字
    "ai_feedback": str,      # 固定的說明文字
    "analysis_details": {
        "content_length": int, "effective_length": int, "repetition_ratio": float,
        "hesitation_count": int, "sentence_marker_count": int,
        "person_matches": [...], "location_matches": [...], "event_matches": [...],
        "object_matches": [...], "emotion_matches": [...], "time_matches": [...],
        "connector_matches": [...],
        "information_dimension_count": int, "naming_count": int,
    },
}
```

`analysis_details` 是除錯用欄位，方便開發時查看「為什麼得這個分數」，目前沒有存進資料庫。

`format_analysis_for_radar(analysis_result)` 把上面的結果轉成前端雷達圖需要的 `radar_data` 陣列格式 + `summary` 摘要，供 `dashboard.html` 使用。

---

## 6. 與其他檔案的關聯

- **`Voice/models.py` — `CognitiveAnalysis`**：新增 `is_valid`（樣本是否有效）、`invalid_reason`（`too_short` / `suspect_hallucination` / `low_confidence`）；六項分數、`total_score`、`average_score`、`risk_level` 都改成 `null=True`，無效樣本不會寫入誤導性的 0 分。
- **`Voice/views.py` — `vision_firstQ`**：聊天室進頁時呼叫，用 OpenAI vision 生成 `photo_description` 與首問，建立 `AiConversation`；**不涉及 `Voice/utils.py`**，跟語言分析無關。
- **`Voice/views.py` — `update_diary_audio` / `upload_chat_voice`**：聊天室第 1～4 輪，各自把 Whisper 轉錄結果存進 `AiConversation.messages`，**都不會呼叫 `assess_sample_quality` / `analyze_transcription`**——分析被延後到 4 輪都結束後才做一次。
- **`Voice/views.py` — `finalize_diary`**：聊天室按下「生成日記」時觸發。把 `AiConversation.messages` 裡 4 段 `role="user"` 的內容合併成一段文字，寫回 `diary.transcription` / `diary.diary_text`，接著才呼叫 `assess_sample_quality` → `analyze_transcription` 建立 `CognitiveAnalysis`；同一次也會生成日記標題與 `Diarypost`（分享貼文、hashtag、分類）。
- **`Voice/views.py` — `dashboard`**：多處查詢 `CognitiveAnalysis` 都加上 `.filter(is_valid=True)`，無效樣本不會混進平均分數與語言活力指數（LVI）估算，詳見 `calc_lvi()` / `calc_lvi_trend()`。
- **`Voice/management/commands/seed_diary.py`**：直接呼叫 `analyze_transcription`（不經過品質門檻、也不經過聊天室流程），用於灌假資料，`is_valid` 沿用預設值 `True`。

---

## 7. 已知限制（刻意不在這次修正範圍內）

- **欄位命名**：`semantic_score` 對應的實際涵義是「語意完整性」而非「正確性」（只有文字、沒有照片比對），欄位名稱維持向後相容未改。
- **量測粒度**：目前的「流暢度」量測的是字數+猶豫詞、「命名能力」量測的是關鍵詞多樣性，跟臨床上嚴謹定義的同名指標（WPM、標準化命名測驗）不完全對應，僅供健康趨勢追蹤參考。
- **尚未實作**：語速（WPM / CIUs/min）、中文斷詞（jieba/CKIP）取代關鍵詞比對、%CIU 近似值、個人歷史基線 z-score（目前的風險等級門檻是全體共用固定值，不會依個人「正常表現」校正）。

> 本檔案分析結果僅供語言表達與健康趨勢追蹤參考，不能作為醫療診斷依據。
