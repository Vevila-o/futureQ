# 聲影日記｜分享互動與對話流程五項增修

> **適用檔案**：`share.html`／`share_page`、`community.html`／`community_page`、`upload_chat_voice`、`finalize_diary`、`vision_firstQ`、新增 `Notification` model
> **文件性質**：開發用技術文件，決賽前工期
> **基準**：以已完成的聊天室重構為前提（`finalize_diary`、`vision_firstQ`、Whisper 單例皆已存在）

---

## 總覽

| # | 功能 | 新增基礎設施 | 工時估 |
|---|---|---|---|
| 1 | share preview 加 AI 建議回覆句 | `Diarypost.suggested_replies` 欄位 | 半天 |
| 2 | 收到加油氣泡 → 站內通知 | **`Notification` model（新）** | 半天 |
| 3 | LINE 分享附文案 | 無（改前端 URL） | 1 小時 |
| 4 | community 每則日記加分享按鈕 | 無（複用 share URL） | 2 小時 |
| 5 | 對話流程：15 秒門檻＋跳過＋錄音回放 | `ChatReplyAudio`（或欄位擴充） | 1–1.5 天 |

建議實作順序：**3 → 4 → 1 → 2 → 5**（由淺入深，5 動到核心流程放最後）。

---

## 1. share.html preview 模式：AI 建議回覆句

### 需求理解（含推斷，請確認）

親友從分享連結打開 `/share/?preview=1`，在語音回覆錄音區塊**上方**顯示 2–3 個根據貼文內容生成的建議句。貼文講海邊 → 「海邊風景一定很美吧！下次帶我一起去～」。定位是**提詞**：親友不知道說什麼時照著唸，不是點擊送出的文字回覆（`VoiceReply` 目前只收語音，若要文字回覆是另一個工程）。

### 1.1 生成時機：finalize 時一次生成，存起來

**不要**在 `share_page` 載入時即時呼叫 OpenAI——preview 頁是給親友看的，每個親友點開都打一次 API 會重蹈 `_generate_share_content` 舊病。正確做法：在 `finalize_diary` 的 `_generate_share_content()` 裡**順便**生成，存進 `Diarypost`：

```python
# models.py — Diarypost 加欄位（需 migration）
suggested_replies = models.JSONField(default=list, blank=True)
# 形如 ["海邊風景一定很美吧！", "看起來好放鬆，下次帶我去～", "你拍的照片真好看！"]
```

```python
# _generate_share_content 內追加一次呼叫（或併入既有貼文生成的同一個 prompt，省一次 API）
prompt = (
    f"以下是一位長輩今天的日記貼文：\n{first_person_text}\n\n"
    "請生成 3 句親友可以回覆長輩的暖心短句，每句 15 字內、口語、"
    "具體呼應貼文內容（不要泛泛的加油）。"
    "用 JSON 陣列回覆：[\"...\",\"...\",\"...\"]"
)
```

**建議併入既有貼文生成的呼叫**（同一個 prompt 要求同時回傳貼文＋hashtag＋建議句的 JSON），一次呼叫拿全部，成本不變。JSON 解析防呆與 fallback（固定三句通用句）比照 vision 首問的做法。

### 1.2 前端（share.html，僅 preview=1 時渲染）

```html
{% if preview and post.suggested_replies %}
<div class="reply-suggestions">
  <p class="suggest-title">💬 不知道說什麼嗎？可以參考：</p>
  {% for s in post.suggested_replies %}
    <button type="button" class="suggest-chip" onclick="fillSuggestion(this)">{{ s }}</button>
  {% endfor %}
</div>
{% endif %}
```

chip 點擊行為建議：把該句**放大顯示在錄音鈕旁**當提詞卡（親友照著唸），不自動開始錄音。長輩的親友也可能是長輩，操作越少越好。

### 1.3 舊資料相容

`suggested_replies` 是新欄位，既有 `Diarypost` 都是空 list。`share_page` 的 `_get_or_generate_share_content` 已有「讀不到就補算」的退路，把建議句生成掛在同一條退路裡即可；或前端對空 list 直接不渲染區塊（最簡單，舊貼文就沒有建議句，可接受）。

---

## 2. 加油氣泡通知

### 2.1 先講清楚技術現實

系統**沒有** WebSocket（無 channels）、**沒有**背景工作（無 celery）、LINE Notify 已停止服務。所以「即時推播到長輩手機」本週做不到，也不必做。務實方案：**站內通知**——親友送出氣泡時寫一筆通知，長輩下次打開任何頁面時看到「有收到新的加油氣泡喔」。

對長輩的使用情境這已足夠：他們本來就是每天打開 App 寫日記，打開時看到通知的體驗差距不大。

### 2.2 Model（需 migration）

```python
class Notification(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name="notifications")
    kind = models.CharField(max_length=32, default="voice_reply")   # 預留其他通知類型
    diary = models.ForeignKey(DiaryEntry, null=True, on_delete=models.CASCADE)
    message = models.CharField(max_length=100)
    is_read = models.BooleanField(default=False)
    created_at = models.DateTimeField(auto_now_add=True)
```

### 2.3 寫入點：`api_voice_reply`

親友上傳語音成功後：

```python
Notification.objects.create(
    user=diary.user,
    kind="voice_reply",
    diary=diary,
    message="有收到新的加油氣泡喔 🎈",
)
```

### 2.4 顯示：base template 的導覽列

所有登入後頁面共用的 base template 加小鈴鐺＋未讀數：

```python
# context processor（settings.py 註冊），全站頁面自動帶入
def unread_notifications(request):
    if request.user.is_authenticated:
        qs = request.user.notifications.filter(is_read=False)
        return {"unread_count": qs.count(), "unread_items": qs[:5]}
    return {}
```

點鈴鐺 → 下拉列出通知，點某則 → 標記已讀＋導向該日記的 share/日記頁。另外可在**登入後首頁**彈一次 toast（「有收到新的加油氣泡喔」），對長輩比小紅點更明確。

### 2.5 未來規劃（寫進計畫書，本週不做）

- **Web Push**（Service Worker + VAPID）：真推播，但 iOS Safari 需加入主畫面才支援，對長輩門檻高。
- **LINE Messaging API push**：透過官方帳號主動推 LINE 訊息給長輩——這與 memory 中既有的「LINE OA Webhook 未來規劃」是同一條線，寫在一起。

---

## 3. LINE 分享附文案

### 3.1 換分享端點

目前若用的是 LINE 的 `share?url=` 端點，只能帶網址。改用 **`https://line.me/R/share?text=`**，text 內可同時放文案＋網址：

```javascript
function shareToLine(userName, postSummary, shareUrl) {
  const text = `${userName}今天的聲影日記發布囉～\n「${postSummary}」\n快來聽聽並留一句加油吧 🎈\n${shareUrl}`;
  const lineUrl = `https://line.me/R/share?text=${encodeURIComponent(text)}`;
  window.open(lineUrl, '_blank');
}
```

### 3.2 文案組成

- `userName`：`user.first_name` 或顯示名，模板直接注入。
- `postSummary`：取 `Diarypost.first_person_text` **前 20 字**＋「…」。整篇塞進去會讓 LINE 訊息太長，且**剝奪點連結的動機**——文案是釣子，內容留在連結裡。
- `shareUrl`：現有的 preview 連結（含 `?preview=1`）。

### 3.3 注意

- `encodeURIComponent` 必做，中文＋換行不編碼會斷。
- 換行用 `\n`，LINE 支援。
- 手機上 `line.me/R/share` 會喚起 LINE App；桌機會開網頁版。demo 用手機展示效果最好。

---

## 4. community.html 每則日記加分享按鈕

### 4.1 做法：複用，不新建

分享能力已存在（share.html 的 LINE 按鈕＋連結複製），community 只是**多一個入口**。每張日記卡片加一顆分享鈕：

```html
{% for diary in diaries %}
  <div class="diary-card">
    ...現有內容...
    {% if diary.diary_posts.first %}
    <button type="button" class="btn-share-sm"
            onclick="shareToLine('{{ user_display_name }}',
                                 '{{ diary.diary_posts.first.first_person_text|truncatechars:20|escapejs }}',
                                 '{{ request.scheme }}://{{ request.get_host }}/share/?diary_id={{ diary.id }}&preview=1')">
      📤 分享
    </button>
    {% endif %}
  </div>
{% endfor %}
```

`shareToLine()` 抽成共用 JS（share.html 與 community.html 都引），文案格式自動一致（呼應 #3）。

### 4.2 兩個要確認的點

1. **share URL 是否本來就以 `diary_id` 定位**：`share_page` 若目前只顯示「最新一篇」，舊日記的分享連結會指錯內容。確認 `share_page` 接受 `diary_id` 參數並據此取貼文；沒有的話這是本項的隱藏工作量（加參數＋查詢，約 1 小時）。
2. **舊日記可能沒有 `Diarypost`**（finalize 之前的資料）：模板已用 `{% if diary.diary_posts.first %}` 防呆，沒有貼文就不顯示分享鈕，或顯示灰色鈕提示「此篇尚未生成分享內容」。

### 4.3 「重複分享」的語意

分享本身無狀態（就是開 LINE 視窗），同一篇按幾次都行，不需要額外處理。唯一注意：**不要**在每次分享時重新生成貼文——`Diarypost` 已存好，直接讀。

---

## 5. 對話流程調整：15 秒門檻＋跳過＋錄音回放

這是五項中最重的，動到聊天室核心。拆成三個子需求。

### 5.0 規則總表（先對齊認知）

| 輪次 | 最短時長 | 可跳過 | 音檔保存 |
|---|---|---|---|
| 第 1 次（描述照片） | **15 秒**，不足要求重錄 | ✕ | 永久（現況） |
| 第 2 次 | 無 | ✕ | **暫存**，finalize 後刪 |
| 第 3 次 | 無 | ✓（跳過鈕） | 暫存，finalize 後刪 |
| 第 4 次 | 無 | ✓ | 暫存，finalize 後刪 |

「生成日記」button 出現條件：**已完成 ≥2 次回覆**（第 2 次答完即可出現，與第 3/4 的跳過鈕並存；或答滿 4 次／跳過後出現）。

### 5.1 首問 prompt 補強（引導詳述圖片）

`vision_firstQ` 的 vision prompt 調整，讓 Q1 明確引導描述：

```
...再以溫暖口語的語氣，邀請他「仔細描述這張照片」：
照片裡有誰、在哪裡、正在做什麼、當時的心情如何。
問題 40 字內，語氣像關心的晚輩，不像考試。
```

前端第 1 輪錄音鈕旁的固定提示同步改：「**請盡量詳細描述照片裡的人、地點、發生的事**（至少講 15 秒喔）」。

> 這個引導同時是認知分析的取樣設計：人物／地點／事件／情緒正是六向度的關鍵詞類別，引導語直接提高有效語料的命中率。這點可寫進計畫書。

### 5.2 15 秒門檻

**前端計時為主，後端驗證為輔。**

前端：`MediaRecorder` 開始時記 `startTime`，停止時算秒數。

```javascript
const MIN_FIRST_REPLY_SEC = 15;

recorder.onstop = () => {
  const sec = (Date.now() - startTime) / 1000;
  if (isFirstReply && sec < MIN_FIRST_REPLY_SEC) {
    showRetryBubble(
      `再多說一點好嗎？可以講講照片裡有誰、在哪裡、發生了什麼事～（剛剛錄了 ${Math.round(sec)} 秒）`
    );
    resetRecordingUI();   // 丟棄這次錄音，回到可錄音狀態，不上傳
    return;
  }
  uploadRecording(blob);
};
```

後端（`update_diary_audio`）防呆：ffmpeg 轉檔後用 `ffprobe` 讀時長（你已依賴 ffmpeg，`ffprobe` 同套件就有），`< 13` 秒（留 2 秒緩衝）回 `{"error": "too_short", "duration": ...}`，前端顯示同樣的重錄氣泡。

**對長輩的語氣**：重錄提示絕不能像錯誤訊息。用鼓勵句＋具體引導（給「可以講什麼」的提示），不要只說「時間不足請重錄」。

**防呆上限**：連續 3 次都不足 15 秒就放行（記 `is_valid` 風險或如實分析短語料）。真的講不了 15 秒的長輩，卡住他比短語料更糟——這本身也是一個观察訊號。

### 5.3 第 3、4 次可跳過

前端：第 3 問、第 4 問的氣泡旁多一顆「這題跳過」鈕。點擊打新端點：

```python
# /api/skip-chat-round/ POST
def skip_chat_round(request):
    conv = AiConversation.objects.get(diary_id=request.POST["diary_id"])
    user_replies = sum(1 for m in conv.messages if m["role"] == "user")
    if user_replies < 2:
        return JsonResponse({"error": "min_replies_not_met"}, status=400)

    conv.add_message("user", "[使用者選擇跳過此題]")   # 佔位，維持問答配對
    user_replies += 1

    if user_replies >= TARGET_USER_REPLIES:
        conv.is_finished = True
        conv.save()
        return JsonResponse({"done": True})
    q = _generate_followup(conv.messages)
    conv.add_message("assistant", q)
    return JsonResponse({"question": q})
```

**兩個連動修改：**

1. **`finalize_diary` 合併語料時要濾掉佔位**：

```python
user_texts = [
    m["content"] for m in conv.messages
    if m["role"] == "user" and m["content"] != "[使用者選擇跳過此題]"
]
```

2. **「生成日記」button 出現時機**：第 2 次回覆完成後即出現（與第 3 問並存），前端 `done` 判定改成 `user_replies >= 2` 就允許 finalize，`>= 4` 才自動收尾。

> **⚠️ 答辯論述連動**：允許 2 輪結束，合併語料可能不足 CIU 建議的 300–400 詞。說法改成：「系統**鼓勵四輪**（預設流程）、**保底兩輪**（尊重長輩自主），實際樣本量如實記錄於分析中；樣本過短時由品質閘門標記 `is_valid=False`，不產生誤導性分數。」——把彈性講成尊重使用者，把樣本量問題交給既有的品質閘門，論述就閉環了。

### 5.4 第 2–4 次錄音回放（暫存到 finalize 才刪）

**現況**：只有第 1 次錄音存檔（`diary.audio_file`），第 2–4 次轉錄完即丟。

**Model**（需 migration）：

```python
class ChatReplyAudio(models.Model):
    conversation = models.ForeignKey(AiConversation, on_delete=models.CASCADE,
                                     related_name="reply_audios")
    reply_index = models.IntegerField()            # 2 / 3 / 4
    audio_file = models.FileField(upload_to="chat_replies/")
    created_at = models.DateTimeField(auto_now_add=True)
```

**`upload_chat_voice`**：轉檔後（既有 `_convert_audio_to_mp3()` 流程）除了轉錄，也把 mp3 存進 `ChatReplyAudio`，回傳 `audio_url` 給前端。

**前端**：使用者氣泡旁加播放鈕（`<audio>` 或自訂播放器），第 1 次用 `diary.audio_file.url`，第 2–4 次用回傳的 `audio_url`。

**`finalize_diary` 收尾刪除**：

```python
for ra in conv.reply_audios.all():
    ra.audio_file.delete(save=False)   # 刪實體檔
    ra.delete()                        # 刪紀錄
```

**孤兒檔案問題（必須處理）**：長輩錄到第 3 次關掉頁面、永遠不按生成日記 → 音檔永遠留存。無 celery，用 management command＋排程（cron 或手動）：

```python
# management/commands/cleanup_chat_audios.py
# 刪除 created_at 超過 48 小時、且所屬對話未 finalize 的 ChatReplyAudio
```

demo 期間手動跑即可；計畫書可寫「暫存音檔 48 小時自動清除」——這反而是隱私設計的加分敘述。

---

## 6. Migration 清單（一次做完）

| Model | 變更 |
|---|---|
| `Diarypost` | + `suggested_replies` JSONField |
| `Notification` | 新表 |
| `ChatReplyAudio` | 新表 |

三個變更互不依賴，一次 `makemigrations` 完成。

---

## 7. 測試重點

```python
def test_suggested_replies_generated_on_finalize(self):
    """finalize 後 Diarypost.suggested_replies 有 3 句"""

def test_notification_created_on_voice_reply(self):
    """親友上傳語音後，日記主人有一筆未讀通知"""

def test_first_reply_under_15s_rejected(self):
    """第 1 次回覆 10 秒音檔 → 回 too_short，不進對話串"""

def test_skip_requires_two_replies(self):
    """只回覆 1 次就打 skip → 400"""

def test_skipped_rounds_excluded_from_analysis(self):
    """跳過佔位訊息不進合併語料"""

def test_finalize_deletes_temp_audios(self):
    """finalize 後 ChatReplyAudio 為空、實體檔已刪"""

def test_finalize_allowed_after_two_replies(self):
    """2 次回覆後 finalize 成功，分析吃 2 段合併"""
```

---

## 8. 給答辯的一段話（這批功能的整體定位）

> 「這一批更新把單向分享升級成**有來有往的循環**：AI 依貼文內容替親友準備回覆提詞（降低親友端的表達門檻——和我們替長輩降低表達門檻是同一個設計哲學）、長輩收到加油氣泡會得到通知（完成情感回路）、每一篇日記都能隨時再分享（記憶可以反覆流動）。對話流程同時加入 15 秒引導與跳過機制——鼓勵充分表達，但把最終決定權留給長輩。」

「替親友也降低表達門檻」這句是本批功能最漂亮的論述——它把 #1 從小功能升格成設計哲學的延伸，與 bidirectional 迴路（memory 中的 DiaryComment 缺口）形成呼應：**語音加油＋建議提詞＋通知，實質上已經是輕量版的雙向互動**，這正面回應了「分享是單向的」這個一直被標記的弱點。
